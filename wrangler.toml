# Cloudflare Pages / Workers config.
# Import https://github.com/ryank33/shared-gallery-room in the Cloudflare
# dashboard (Workers & Pages → Create → Connect to Git) or run:
#   CF_PAGES=1 npm run build && npx wrangler pages deploy
name = "shared-gallery-room"
compatibility_date = "2026-08-14"
pages_build_output_dir = ".output/public"
